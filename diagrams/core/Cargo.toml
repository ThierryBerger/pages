[package]
name = "diagrams-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true

# Deliberately dependency-free: the projection math is ~40 lines, and keeping
# this crate free of deps is what lets the same code render a build-time SVG
# and a ~40KB wasm module.
[dependencies]
