[workspace]
resolver = "2"
members = ["core", "svg", "wasm"]

[workspace.package]
edition = "2021"
license = "MIT"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
